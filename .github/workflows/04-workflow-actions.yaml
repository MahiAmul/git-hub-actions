name: runner-actions

on: push

jobs:
  runner-ubuntu:
    name: runner-actions
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v3
      - name: print folder
        run: ls
